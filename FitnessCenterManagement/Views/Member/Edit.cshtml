@model FitnessCenterManagement.Models.Member

@{
    ViewData["Title"] = "Edit";
}

<style>
    /* Custom styling for the Edit page */
    h1, h4 {
        color: #e74c3c;
    }

    hr {
        border-color: #3498db; /* Horizontal rule color */
    }

    .form-group {
        margin-bottom: 15px;
    }

    label {
        color: #e74c3c; /* Label color */
        font-weight: bold;
    }

    .form-control {
        border: 1px solid #3498db; /* Form control border color */
        border-radius: 4px;
        width: 100%;
        padding: 8px;
        box-sizing: border-box;
    }

        .form-control:focus {
            border-color: #e74c3c; /* Form control border color on focus */
        }

    .btn-primary {
        background-color: #e74c3c; /* Primary button background color */
        color: #fff; /* Primary button text color */
        border: 1px solid #e74c3c; /* Primary button border color */
    }

        .btn-primary:hover {
            background-color: #3498db; /* Primary button background color on hover */
            border-color: #3498db; /* Primary button border color on hover */
        }

    .text-danger {
        color: #e74c3c; /* Text color for validation messages */
    }

    a:hover {
        color: #3498db; /* Link color */
    }

        a {
            color: #e74c3c; /* Hover link color */
        }
</style>

<h1>Edit</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="MemberID" />
            <div class="form-group">
                <label asp-for="FirstName" class="control-label">First Name</label>
                <input asp-for="FirstName" class="form-control" />
                <span asp-validation-for="FirstName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="LastName" class="control-label">Last Name</label>
                <input asp-for="LastName" class="form-control" />
                <span asp-validation-for="LastName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="MembershipType" class="control-label">Membership Type</label>
                <input asp-for="MembershipType" class="form-control" />
                <span asp-validation-for="MembershipType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ExpirationDate" class="control-label">Expiration Date</label>
                <input asp-for="ExpirationDate" class="form-control" />
                <span asp-validation-for="ExpirationDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
